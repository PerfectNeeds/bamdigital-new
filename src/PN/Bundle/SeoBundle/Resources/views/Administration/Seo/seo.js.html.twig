<script>
    var focusKeywordUrlAjax = '{{ path('fe_check_focus_keyword_ajax') }}';
    var checkSlugIsUsedUrlAjax = '{{ path('fe_check_slug_ajax') }}';
    var seoId = {% if entity.seo.id is defined %}'{{ entity.seo.id }}'{% else %}null{% endif %};    
    var seoBaseRouteId = {{ seoBaseRoute.id }};
    var descriptionId = {% if descriptionId is defined %}'{{ descriptionId }}'{% else %}null{% endif %};
    var pageTitle = {% if pageTitleId is not null %}$('#' + '{{ pageTitleId }}'){% else %}null{% endif %};
    var seoFromId = {% if seoFromId is not null %}'{{ seoFromId }}'{% else %}null{% endif %};
</script>
<script src="{{ asset('bundles/seo/pn.seo.js') }}"></script>
<link href="{{ asset('bundles/seo/pn.style.seo.css') }}" rel="stylesheet" type="text/css" />
