{% extends 'base.html.twig' %}

{% block body %}
    <h1>Seopage</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ seoPage.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ seoPage.title }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('seopage_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('seopage_edit', { 'id': seoPage.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
